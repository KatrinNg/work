import palette from '../../palette';

export default {
    root: {
        '&$error': {
            color: palette.error.main,
            // padding: '2px 14px',
            fontSize: 12
        }
    }
};