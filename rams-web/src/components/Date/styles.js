import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    root: {
        borderRadius:8
    },
    textField: () => ({
        paddingTop: 0,
        paddingBottom: 0,
        height: 40,
    }),
    
}));